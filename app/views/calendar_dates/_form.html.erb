<%= form_for(calendar_date) do |f| %>
  <% if calendar_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calendar_date.errors.count, "error") %> prohibited this calendar_date from being saved:</h2>

      <ul>
      <% calendar_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :service_id %>
    <%= f.number_field :service_id %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :exception_type %>
    <%= f.number_field :exception_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
