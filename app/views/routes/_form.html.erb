<%= form_for(route) do |f| %>
  <% if route.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(route.errors.count, "error") %> prohibited this route from being saved:</h2>

      <ul>
      <% route.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :route_id %>
    <%= f.number_field :route_id %>
  </div>

  <div class="field">
    <%= f.label :route_short_name %>
    <%= f.text_field :route_short_name %>
  </div>

  <div class="field">
    <%= f.label :route_long_name %>
    <%= f.text_field :route_long_name %>
  </div>

  <div class="field">
    <%= f.label :route_type %>
    <%= f.number_field :route_type %>
  </div>

  <div class="field">
    <%= f.label :route_color %>
    <%= f.text_field :route_color %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
